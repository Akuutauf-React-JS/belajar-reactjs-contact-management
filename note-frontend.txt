web frontend template
- https://github.com/ProgrammerZamanNow/web-template-contact-management

running backend :
- node src/main.js

setup project
- npm create vite@latest belajar-reactjs-contact-management -- --template react
- npm install 
- npm install react-router

react use
- salah satu hooks library yang populer di react adalah react-use, yang berisi hooks yang bisa kita gunakan dengan mudah
- pada materi ini, kita akan menggunakan library ini juga untuk membantu mempermudah pembuatan project
- referensi : https://github.com/streamich/react-use
- install library react use : npm install react-use

sweet alert
- library lain yang akan kita gunakan di project ini adalah sweet alert, ini adalah library yang bisa kita gunakan untuk menampilkan alert yang lebih baik dibandingkan alert bawaan dari browser
- referensi : https://sweetalert2.github.io/
- npm install sweetalert2

challenge 

implement index path (sudah)
- buat halaman path /
- cek jika user belum login, redirect ke halaman login (cek token, get user)
- jika user sudah login, redirect ke halaman dashboard contacts

validasi session di dashboard
- di semua halaman /dashboard
- validasi jika user belum login, redirect ke halaman login

tailwind css
- perbaiki cara menggunakan tailwind css
- referensi : https://tailwindcss.com/docs/installation/using-vite (tidak menggunakan cdn)

reusable component
- pisahkan beberapa bagian yang bisa dijadikan component sehingga bisa digunakan ulang tanpa harus membuat kode yang sama
- misal, seperti Contact Card, Contact Form (Create/Edit), Address Form (Create & Edit), Paging dan lain lain

Deploy 
- deploy aplikasi frontend dan backend di server, agar bisa diakses via internet

